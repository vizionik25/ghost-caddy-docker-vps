# ===========================================
# Ghost Blog Production Configuration
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# Domain Configuration (REQUIRED)
# ===========================================
# Your actual domain name (without https://)
DOMAIN=yourdomain.com

# Full URL of your Ghost installation
GHOST_URL=https://yourdomain.com

# Email for Let's Encrypt SSL certificate notifications
ACME_EMAIL=admin@yourdomain.com

# ===========================================
# Ghost Configuration
# ===========================================
# Environment: production or development
NODE_ENV=production

# ===========================================
# Mail Configuration (for newsletters)
# ===========================================
# Transport: Direct, SMTP
MAIL_TRANSPORT=SMTP
MAIL_FROM=noreply@yourdomain.com

# SMTP Settings (required for newsletters)
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=your-mailgun-user
SMTP_PASS=your-mailgun-password
SMTP_SECURE=false

# ===========================================
# Popular SMTP Providers
# ===========================================
# 
# Mailgun (Recommended by Ghost):
#   SMTP_HOST=smtp.mailgun.org
#   SMTP_PORT=587
#
# SendGrid:
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_PORT=587
#
# Amazon SES:
#   SMTP_HOST=email-smtp.us-east-1.amazonaws.com
#   SMTP_PORT=587
#
# Gmail (not recommended for production):
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
